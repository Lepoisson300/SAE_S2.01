classe Window: 
	ajout de la fonction SelectCard
	ajout de la fonction Window_MouseDown
	suppression de la fonction PlaceAllButton

classe Cardview:
	fonction GetRectangle


classe controller :
	attribut CurrentPlayer devient public (précedemment privé)
	supression de la fonction PlaceAllCards

classe Game:
	ajout de l'attribut discardStack
	ajout de la fonction shuffle
	attribut commonDeck devient public (précedemment privé)